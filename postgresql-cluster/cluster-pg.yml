apiVersion: "acid.zalan.do/v1"
kind: postgresql
metadata:
  name: pg-cluster
  namespace: postgres-cluster
spec:
  teamId: "pg-cluster"
  volume:
    size: 10Gi
  numberOfInstances: 2
  users:
    root:
      - superuser
      - createdb
  postgresql:
    version: "17"
