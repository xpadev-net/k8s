apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: osk-cloudflared-tunnel-app
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/xpadev-net/k8s.git
    targetRevision: master
    path: common/cloudflared/overlays/osk
  destination:
    server: https://kubernetes.default.svc
    namespace: cloudflared
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    retry:
      limit: 5
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m
    syncOptions:
      - CreateNamespace=true
